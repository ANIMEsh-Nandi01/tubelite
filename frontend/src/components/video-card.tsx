import Link from "next/link";
import { formatDuration, formatViews, Video } from "@/lib/api";

export function VideoCard({ video }: { video: Video }) {
  return <article className="group"><Link href={`/watch/${video.id}`} className="relative block aspect-video overflow-hidden rounded-2xl bg-gradient-to-br from-indigo-500/60 via-violet-800/30 to-zinc-800"><div className="absolute inset-0 bg-[radial-gradient(circle_at_70%_20%,rgba(255,255,255,.22),transparent_25%)] transition duration-500 group-hover:scale-110" />{video.thumbnail_url ? <img className="h-full w-full object-cover" src={video.thumbnail_url} alt="" /> : <span className="absolute bottom-4 left-4 text-3xl font-black text-white/90">{video.title.charAt(0)}</span>}<span className="absolute bottom-2 right-2 rounded bg-black/80 px-1.5 py-0.5 text-xs font-medium">{formatDuration(video.duration)}</span></Link><div className="mt-3 flex gap-3"><div className="grid h-9 w-9 shrink-0 place-items-center rounded-full bg-indigo-400/20 text-sm font-bold text-indigo-200">{video.author.username.charAt(0)}</div><div className="min-w-0"><Link href={`/watch/${video.id}`} className="line-clamp-2 font-semibold leading-5 transition hover:text-indigo-300">{video.title}</Link><p className="mt-1 text-sm text-zinc-400">{video.author.username}</p><p className="text-sm text-zinc-500">{formatViews(video.view_count)} views</p></div></div></article>;
}
